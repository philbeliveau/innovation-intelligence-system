# Story 11.6.1: Prompt Download, Upload & Validation UI

**Epic:** Epic 11.6 - Custom Prompt Upload System
**Priority:** P1
**Estimated Time:** 8 hours

---

## Status

**Draft**

---

## Story

**As a** CPG innovation founder (Lactalis, McCormick, Decathlon),
**I want** to download default prompt templates, upload customized `.md` files for pipeline stages, and receive immediate validation feedback,
**so that** I can iterate on prompt strategies without writing code and get clear feedback when my prompts are incomplete.

---

## Acceptance Criteria

1. New "📂 Custom Prompts" tab in Gradio UI with two sections: Download Section (7 individual download buttons Stage 0-6 + "Download ALL (ZIP)" button) and Upload Section (7 file upload components)
2. ZIP download includes all 7 default templates (stage_0_enrichment.md through stage_6_packaging.md) + README.md with placeholder documentation
3. 7 file upload components (`gr.File(file_types=[".md"])`) labeled "Stage X Custom Prompt (optional)"
4. File size limit enforced (max 1MB per file)
5. Help text displayed: "Leave blank to use default prompt"
6. "✅ Validate Uploaded Prompts" button triggers client-side validation
7. Validation checks each uploaded file for required placeholders using regex pattern matching (`\{[a-z_]+\}`)
8. Validation results display per-stage status: ✅ "Stage 4: Valid (filename.md)", ❌ "Stage 4: MISSING placeholders: {insights}, {brand_context}", ℹ️ "Stage 0: Using default prompt (no file uploaded)"
9. Invalid files highlighted in red, blocking pipeline execution
10. Uploaded files persist in `gr.State()` during session
11. File content extraction: When file uploaded via gr.File component, read content with `open(file.name, 'r', encoding='utf-8')`, store in gr.State() with structure: `{"stage_4": {"filename": "stage_4_v1.md", "content": "Full markdown content...", "uploaded_at": "2025-11-24T10:30:00Z"}}`
12. Event handler for file upload updates gr.State() immediately
13. If user uploads new file for same stage, replace existing entry
14. Existing Gradio UI tabs unaffected (Main, Results, Experiment History tabs remain functional)
15. Pipeline execution flow unchanged when no custom prompts uploaded (backward compatible)
16. Modify run_pipeline() method signature (line 440) to add `custom_prompts: Optional[Dict[str, str]] = None` parameter
17. Modify HTTP request to /run/local (lines 487-493) to conditionally include custom_prompts field in payload
18. Modify run_pipeline_wrapper() (line 1385) to extract custom prompts from gr.State() and pass to run_pipeline() method

---

## Tasks / Subtasks

- [ ] Task 1: Create "Custom Prompts" tab in Gradio UI (AC: 1)
  - [ ] Add new tab after "Example Curation" tab (line 1149 in gradio_lab.py)
  - [ ] Create gr.Markdown header: "## 📂 Custom Prompt Templates"
  - [ ] Add two gr.Accordion sections: "📥 Download Templates" and "📤 Upload Custom Prompts"

- [ ] Task 2: Implement prompt template download functionality (AC: 2)
  - [ ] Create `resolve_prompts_dir()` helper function for multi-environment path resolution
  - [ ] Create 7 individual download buttons (Stage 0-6)
  - [ ] Implement `create_default_template_zip()` function using zipfile module
  - [ ] Generate README.md with placeholder documentation table
  - [ ] Wire download buttons to gr.File outputs

- [ ] Task 3: Implement file upload components (AC: 3, 4, 5, 10)
  - [ ] Create 7 gr.File components with `file_types=[".md"]`
  - [ ] Add labels: "Stage X Custom Prompt (optional)"
  - [ ] Add help text: "Leave blank to use default prompt"
  - [ ] Set file size limit: `max_file_size=1_000_000` (1MB)

- [ ] Task 4: Implement file content extraction and storage (AC: 11, 12, 13)
  - [ ] Create file upload event handler
  - [ ] Extract content: `with open(file.name, 'r', encoding='utf-8') as f: content = f.read()`
  - [ ] Store in cached_data gr.State(): `{"stage_X": {"filename": ..., "content": ..., "uploaded_at": ...}}`
  - [ ] Handle file replacement for same stage

- [ ] Task 5: Implement client-side validation (AC: 6, 7, 8, 9)
  - [ ] Create "✅ Validate Uploaded Prompts" button
  - [ ] Implement `validate_placeholders()` function with regex: `\{[a-z_]+\}`
  - [ ] Load required placeholders per stage from placeholder requirements table
  - [ ] Display validation results with status icons (✅❌ℹ️)
  - [ ] Highlight invalid files in red
  - [ ] Disable pipeline execution button if validation errors

- [ ] Task 6: Modify run_pipeline() method for backend integration (AC: 16, 17)
  - [ ] Add `custom_prompts: Optional[Dict[str, str]] = None` parameter to method signature (line 440)
  - [ ] Modify HTTP request payload construction (lines 487-493)
  - [ ] Conditionally include custom_prompts field: `if custom_prompts: payload["custom_prompts"] = custom_prompts`

- [ ] Task 7: Modify run_pipeline_wrapper() (AC: 18)
  - [ ] Extract custom_prompts_data from gr.State() (line 1385)
  - [ ] Convert to dict of content strings only (exclude metadata)
  - [ ] Pass custom_prompts to run_pipeline() method
  - [ ] Handle None case for backward compatibility

- [ ] Task 8: Testing (AC: 14, 15)
  - [ ] Unit tests for placeholder validation logic
  - [ ] Integration test: upload valid file → validation passes → file content in gr.State()
  - [ ] Integration test: upload invalid file → validation fails → pipeline button disabled
  - [ ] Regression test: pipeline execution with NO custom prompts uses default templates

---

## Dev Notes

### Relevant Source Tree Info

**Files to Modify:**
- `backend/experimentation/gradio_lab.py` (1,666 lines)
  - Line 950-958: Session state (add `"custom_prompts": {}` field)
  - Line 1149: Tab insertion point (after Example Curation tab)
  - Line 440: run_pipeline() method signature
  - Line 487-493: HTTP request to /run/local
  - Line 1385: run_pipeline_wrapper() function

**Files to Reference:**
- `backend/experimentation/prompts/` directory (7 default templates)
  - stage_0_enrichment.md
  - stage_1_extraction.md
  - stage_2_convergence.md
  - stage_3_technique_matching.md
  - stage_4_concept_generation.md
  - stage_5_competitive_search.md
  - stage_6_packaging.md

**Integration Points:**
- Gradio UI → FastAPI /run/local endpoint (Story 11.6.2 consumes custom_prompts field)
- gr.State() → save_experiment() (Story 11.6.3 persists custom_prompts metadata)

### Path Resolution Pattern (Multi-Environment Support)

```python
def resolve_prompts_dir() -> Path:
    """Resolve prompts directory across deployment environments"""
    possible_paths = [
        Path("/app/experimentation/prompts"),       # HF Spaces absolute
        Path("prompts"),                             # Local (same dir as gradio_lab.py)
        Path("backend/experimentation/prompts"),    # Project root
    ]
    for path in possible_paths:
        if path.exists():
            return path
    raise FileNotFoundError("Prompts directory not found")
```

### Placeholder Requirements Table

| Stage | Required Placeholders |
|-------|-----------------------|
| Stage 0 | `{company_name}`, `{industry}`, `{portfolio}` |
| Stage 1 | `{report_text}` |
| Stage 2 | `{trend_data}`, `{brand_context}` |
| Stage 3 | `{insights}`, `{brand_resources}` |
| Stage 4 | `{insights}`, `{matched_techniques}`, `{brand_context}`, `{no_hallucination_rules}` |
| Stage 5 | `{initiatives}`, `{brand_context}` |
| Stage 6 | `{all_stage_outputs}` |

### Verified Placeholder Format

From existing `stage_4_concept_generation.md`:
- `{few_shot_examples}` - Few-shot learning examples
- `{no_hallucination_rules}` - Hallucination prevention rules
- `{insights}` - Consumer insights from Stage 2
- `{matched_techniques}` - SIT techniques from Stage 3
- `{brand_context}` - Brand profile data

### Existing Pattern References

- **PDF upload pattern**: Lines 1027-1036 in gradio_lab.py (`gr.File()` with validation feedback)
- **Validation feedback pattern**: Display status messages with icons (✅❌ℹ️)
- **Multi-environment path resolution**: Lines 91-100 in gradio_lab.py

### Scope Boundary

This story implements UI ONLY:
- ✅ Download templates
- ✅ Upload custom prompts
- ✅ Client-side validation
- ✅ Store in gr.State()
- ✅ Modify run_pipeline() to send custom_prompts to backend

Does NOT implement:
- ❌ Backend validation (Story 11.6.2)
- ❌ Backend prompt usage (Story 11.6.2)
- ❌ Database storage (Story 11.6.3)

### Key Constraints

- File size limit: 1MB per file (prevents accidental upload of large files)
- Client-side validation only (server-side validation in Story 11.6.2)
- Validation does NOT check prompt quality, only placeholder presence
- Must work in HF Spaces, Railway, and local development environments

### Testing

**Test File Location:**
- `backend/tests/experimentation/test_gradio_lab.py`

**Testing Standards:**
- Framework: pytest
- Coverage target: 90%+ for new code
- Pattern: Arrange-Act-Assert

**Unit Tests Required:**
- `test_validate_placeholder_presence()` - Valid file passes
- `test_validate_missing_placeholders()` - Invalid file fails with correct error
- `test_validate_empty_file()` - Empty file fails
- `test_create_default_template_zip()` - ZIP creation includes all 7 templates + README
- `test_resolve_prompts_dir()` - Multi-environment path resolution

**Integration Tests Required:**
- `test_upload_valid_file_validation_passes()` - Upload valid file → validation passes → file content in gr.State()
- `test_upload_invalid_file_validation_fails()` - Upload file missing placeholders → validation fails → pipeline button disabled
- `test_upload_no_files_default_behavior()` - No files uploaded → pipeline uses default templates (regression test)

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-24 | 1.0 | Initial story creation from Epic 11.6 | John (PM Agent) |

---

## Dev Agent Record

### Agent Model Used

_To be filled by dev agent during implementation_

### Debug Log References

_To be filled by dev agent during implementation_

### Completion Notes

_To be filled by dev agent during implementation_

### File List

_To be filled by dev agent during implementation_

---

## QA Results

_To be filled by QA agent after story completion_
